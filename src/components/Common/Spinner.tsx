import React from "react";
import { CircularProgress } from "@mui/material";
const Spinner = () => {
  return (
    <div>
      <CircularProgress
        // color="neutral"
        // determinate={false}
        size="lg"
        // value={26}
        // variant="soft"
      />
    </div>
  );
};

export default Spinner;
